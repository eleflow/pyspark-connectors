import os
import pipe_drive.services as service

class ActiveCampaignServiceClient:

    
    def __init__(self, api_token, base_url) -> None:
        """ Initialize the PipeDriveServiceClient.
        """
        # Create the service clients
        
